# Jupyter & OS clutter
.ipynb_checkpoints/
.DS_Store
Thumbs.db

# Keys / env (never commit)
alpha.key
*.key
.env
notebooks/api/*.txt

# Power BI binaries are usually large; keep screenshots instead
*.pbix

